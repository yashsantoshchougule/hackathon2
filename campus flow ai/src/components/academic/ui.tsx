import type { ReactNode } from 'react'
import type { AcademicApiError, AttendanceRisk, Priority, VerificationStatus } from '../../types/academic'
import './ui.css'

export type IconName = 'grid' | 'checklist' | 'calendar' | 'cap' | 'chart' | 'bell' | 'lock' | 'help' | 'menu' | 'chevron' | 'plus' | 'arrow' | 'clock' | 'book' | 'alert' | 'refresh' | 'dots' | 'check' | 'filter' | 'target' | 'location' | 'close' | 'spark'
const paths: Record<IconName, string> = {
  grid:'M4 4h6v6H4zM14 4h6v6h-6zM4 14h6v6H4zM14 14h6v6h-6z', checklist:'M9 6h11M9 12h11M9 18h11M4 6h.01M4 12h.01M4 18h.01', calendar:'M5 3v3m14-3v3M4 9h16M5 5h14a1 1 0 011 1v13a1 1 0 01-1 1H5a1 1 0 01-1-1V6a1 1 0 011-1z', cap:'M3 9l9-5 9 5-9 5-9-5zm3 3v4c0 1.7 2.7 3 6 3s6-1.3 6-3v-4', chart:'M4 19V5m0 14h16M8 16v-4m4 4V8m4 8v-6m4 6V6', bell:'M18 9a6 6 0 00-12 0c0 7-3 7-3 9h18c0-2-3-2-3-9m-8 12h4', lock:'M7 10V7a5 5 0 0110 0v3m-11 0h12v10H6V10z', help:'M9.1 9a3 3 0 115.7 1.3c-.9.9-2.8 1.3-2.8 3.2m.01 3.5h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z', menu:'M4 7h16M4 12h16M4 17h16', chevron:'M9 18l6-6-6-6', plus:'M12 5v14M5 12h14', arrow:'M5 12h14m-6-6l6 6-6 6', clock:'M12 7v5l3 2m6-2a9 9 0 11-18 0 9 9 0 0118 0z', book:'M5 4.5A2.5 2.5 0 017.5 2H20v17H7.5A2.5 2.5 0 005 21.5v-17zm0 0V19m0 0a2.5 2.5 0 012.5-2.5H20', alert:'M12 9v4m0 4h.01M10.3 3.9L2.8 17a2 2 0 001.7 3h15a2 2 0 001.7-3L13.7 3.9a2 2 0 00-3.4 0z', refresh:'M20 11a8 8 0 00-14.9-4L3 9m0-5v5h5m-4 4a8 8 0 0014.9 4L21 15m0 5v-5h-5', dots:'M5 12h.01M12 12h.01M19 12h.01', check:'M5 12l4 4L19 6', filter:'M4 5h16M7 12h10m-7 7h4', target:'M12 12m-8 0a8 8 0 1016 0 8 8 0 10-16 0m4 0a4 4 0 108 0 4 4 0 10-8 0m4 0h.01', location:'M20 10c0 5-8 11-8 11S4 15 4 10a8 8 0 1116 0zm-8 3a3 3 0 100-6 3 3 0 000 6z', close:'M6 6l12 12M18 6L6 18', spark:'M12 3l1.7 5.3L19 10l-5.3 1.7L12 17l-1.7-5.3L5 10l5.3-1.7L12 3zm6.5 12l.7 2.3 2.3.7-2.3.7-.7 2.3-.7-2.3-2.3-.7 2.3-.7.7-2.3z'
}
export function Icon({ name, size = 20 }: { name: IconName; size?: number }) { return <svg width={size} height={size} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round" aria-hidden="true"><path d={paths[name]} /></svg> }
export function PageIntro({ eyebrow, title, children, action }: { eyebrow?: string; title: string; children?: ReactNode; action?: ReactNode }) { return <div className="page-intro"><div><div className="eyebrow">{eyebrow || 'ACADEMICS'}</div><h1>{title}</h1>{children && <p>{children}</p>}</div>{action && <div className="intro-action">{action}</div>}</div> }
export function Button({ children, icon, kind = 'primary', className = '', ...props }: React.ButtonHTMLAttributes<HTMLButtonElement> & { children: ReactNode; icon?: IconName; kind?: 'primary' | 'secondary' | 'quiet' | 'danger'; className?: string }) { return <button className={`button button-${kind} ${className}`} {...props}>{icon && <Icon name={icon} size={17} />}{children}</button> }
export function VerifyBadge({ status }: { status: VerificationStatus }) { const label = status === 'verified' ? 'Verified' : status === 'student_confirmed' ? 'Personal' : status === 'pending_confirmation' ? 'Needs confirmation' : status === 'unverified' ? 'Unverified' : 'Rejected'; return <span className={`verification verification-${status}`}>{status === 'verified' && <Icon name="check" size={12} />}{label}</span> }
export function RiskBadge({ risk }: { risk: AttendanceRisk }) { return <span className={`risk risk-${risk}`}>{risk === 'safe' ? 'Safe' : risk === 'warning' ? 'At risk' : risk === 'critical' ? 'Critical' : 'No data'}</span> }
export function PriorityBadge({ priority }: { priority: Priority }) { return <span className={`priority priority-${priority}`}>{priority}</span> }
export function DataState({ error, onRetry, itemName, children }: { error?: AcademicApiError | null; onRetry?: () => void; itemName: string; children?: ReactNode }) { if (!error) return <div className="empty-state"><div className="state-icon"><Icon name="book" size={24} /></div><h3>No {itemName} yet</h3><p>{children || `When academic records are available, your ${itemName} will appear here.`}</p></div>; const auth = error.status === 401; return <div className="empty-state error-state"><div className="state-icon"><Icon name={auth ? 'lock' : 'alert'} size={24} /></div><h3>{auth ? 'Your session is required' : 'We could not load this information'}</h3><p>{auth ? 'Sign in through CampusFlow to view your protected academic records. We never show placeholder academic data.' : error.message}</p>{!auth && onRetry && <Button kind="secondary" icon="refresh" onClick={onRetry}>Try again</Button>}</div> }
export function LoadingCards({ count = 4 }: { count?: number }) { return <div className="skeleton-grid">{Array.from({ length: count }, (_, index) => <div className="skeleton-card" key={index}><span></span><b></b><i></i></div>)}</div> }
export function StatCard({ label, value, helper, icon, tone = 'green' }: { label: string; value: string | number; helper: string; icon: IconName; tone?: 'green' | 'lime' | 'amber' | 'rose' }) { return <article className={`stat-card stat-${tone}`}><div className="stat-icon"><Icon name={icon} size={20} /></div><div className="stat-label">{label}</div><strong>{value}</strong><small>{helper}</small></article> }
